from typing import Dict, List


def detect_suspicious_patterns(lines: List[str], patterns: List[Dict]) -> List[Dict]:
    """Detect suspicious activity based on regex patterns."""
    pass


def detect_unauthorized_access(lines: List[str]) -> List[Dict]:
    """Detect 401, 403, 500 status codes indicating unauthorized access attempts."""
    pass
